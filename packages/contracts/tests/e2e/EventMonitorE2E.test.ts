describe("Event Monitor E2E", () => {
  it("should monitor multiple contracts simultaneously", async () => {
    // Deploy multiple mock contracts
    // Generate events from different contracts
    // Verify monitoring works across all contracts
  });

  it("should handle network errors gracefully", async () => {
    // Test with network interruptions
    // Verify error handling
  });
});